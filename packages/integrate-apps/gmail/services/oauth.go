package services

// oauth flow

// check if gmail's all scopes are granted, OR if user is authenticated with gmail account.
// if not, redirect to oauth consent page

// generate token after successful oauth consent
// refresh token if expired

// revoke token if user wants to disconnect from gmail account

// User wants to install gmail into buddai
// user will be redirected to google's oauth consent page after clicking on install.
// user will allow emails(both sent and received) to be accessed by BuddAI

// display already connected accounts
// user can connect multiple accounts

func OAuth() {
	
}
