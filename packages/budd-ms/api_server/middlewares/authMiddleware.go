package middlewares


func AuthMiddleware() {
	// AuthMiddleware is a middleware to authenticate the user
	// if the user is not authenticated, it will return 401
	// if the user is authenticated, it will pass the request to the next handler
}